import styles from "./TechnocratzCertificate.module.css";

const TechnocratzCertificate = ({ onClose }) => {
  return (
    <div className={styles.technocratzcertificate}>
      <img
        className={styles.certificatesPortfolio41}
        alt=""
        src="../certificates-portfolio-4-1@2x.png"
      />
    </div>
  );
};

export default TechnocratzCertificate;

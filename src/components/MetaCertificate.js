import styles from "./MetaCertificate.module.css";

const MetaCertificate = ({ onClose }) => {
  return (
    <div className={styles.metacertificate}>
      <img className={styles.image15Icon} alt="" src="../image-15@2x.png" />
    </div>
  );
};

export default MetaCertificate;
